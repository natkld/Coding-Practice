The values of variables a, b, c are given. Evaluate the value of the expression:

 √a +(√|b-c|)/(a^3+b^2+c^4 )

Write a function named expression, which takes the values ​ ​ of the variables a, b, c as arguments and returns the value of the expression rounded to two decimal places.
Examples:
expression (1, 1, 1) must return 1;
expression (6, 1, 7) must return 2.45.

Даны значения переменных a, b, c. Вычислите значение выражения:

√a +(√|b-c|)/(a^3+b^2+c^4 )

Напишите функцию с именем expression, которая принимает значения переменных a, b, c в качестве аргументов и возвращает значение выражения, округленное до двух десятичных знаков.
Примеры:
функция expression(1, 1, 1) должна возвратить 1;
функция expression(6, 1, 7) должна возвратить 2.45.

CODE 1:
function expression(a, b, c){
return +(Math.sqrt(a) + Math.sqrt(Math.abs(b - c)) / (Math.pow(a, 3) + Math.pow(b, 2) + Math.pow(c, 4))).toFixed(2)
}

Все тесты пройдены успешно
PassСоздана функция expression
PassФункция возвращает верный результат 3 для аргументов (9, 12, 0)
PassФункция возвращает верный результат 3.16 для аргументов (10, 16, 7)
PassФункция возвращает верный результат 3.61 для аргументов (13, 1, 18)
PassФункция возвращает верный результат 3.61 для аргументов (13, 7, 17)
PassФункция возвращает верный результат 3.61 для аргументов (13, 13, 9)
PassФункция возвращает верный результат 2.65 для аргументов (7, 2, 12)
PassФункция возвращает верный результат 3.17 для аргументов (10, 18, 2)
PassФункция возвращает верный результат 2.46 для аргументов (6, 18, 3)
PassФункция возвращает верный результат 3.87 для аргументов (15, 16, 19)
PassФункция возвращает верный результат 3.74 для аргументов (14, 8, 5)
PassФункция возвращает верный результат 1.74 для аргументов (3, 11, 3)
PassФункция возвращает верный результат 3.61 для аргументов (13, 9, 13)
PassФункция возвращает верный результат 3.87 для аргументов (15, 17, 8)
PassФункция возвращает верный результат 4.24 для аргументов (18, 10, 18)
PassФункция возвращает верный результат 4.24 для аргументов (18, 5, 8)
PassФункция возвращает верный результат 3.16 для аргументов (10, 0, 4)
PassФункция возвращает верный результат 1 для аргументов (1, 5, 8)
PassФункция возвращает верный результат 4.12 для аргументов (17, 7, 4)
PassФункция возвращает верный результат 2.45 для аргументов (6, 9, 18)
PassФункция возвращает верный результат 4.47 для аргументов (20, 12, 11)
PassФункция возвращает верный результат 1 для аргументов (1, 1, 1)
