Two positive integers a and b are given. a is the length of the corridor, b is the length of the mat. How many mats (without overlapping) is needed to make the entire corridor stand?

rugs

Write a function with the name countOfRugs, which takes the numbers a and b as arguments and returns the maximum number of mats of length b,
necessary to lay down a corridor of length a. Use the Math.floor () method.
Examples:
function countOfRugs (5, 2) must return 2;
function countOfRugs (30, 4) must return 7;
function countOfRugs (3, 4) must return 0.

Даны два целых положительных числа a и b. a - длина коридора, b - длина коврика. Какое количество ковриков (без перекрытия) необходимо, чтобы застелить весь коридор?

rugs

Напишите функцию с именем countOfRugs, которая принимает числа a и b в качестве аргументов и возвращает максимальное количество ковриков длины b, 
необходимое для того, чтобы застелить коридор длиной a. Используйте метод Math.floor().
Примеры:
функция countOfRugs(5, 2) должна возвратить 2;
функция countOfRugs(30, 4) должна возвратить 7;
функция countOfRugs(3, 4) должна возвратить 0.

CODE 1:
function countOfRugs(a, b){
return (Math.floor(a / b))
}

Все тесты пройдены успешно
PassСоздана функция countOfRugs
PassФункция возвращает верный результат 4 для аргументов (4, 1)
PassФункция возвращает верный результат 31 для аргументов (62, 2)
PassФункция возвращает верный результат 8 для аргументов (76, 9)
PassФункция возвращает верный результат 3 для аргументов (37, 10)
PassФункция возвращает верный результат 2 для аргументов (21, 8)
PassФункция возвращает верный результат 3 для аргументов (36, 10)
PassФункция возвращает верный результат 8 для аргументов (50, 6)
PassФункция возвращает верный результат 11 для аргументов (22, 2)
PassФункция возвращает верный результат 10 для аргументов (51, 5)
PassФункция возвращает верный результат 15 для аргументов (63, 4)
PassФункция возвращает верный результат 8 для аргументов (67, 8)
PassФункция возвращает верный результат 15 для аргументов (76, 5)
PassФункция возвращает верный результат 12 для аргументов (37, 3)
PassФункция возвращает верный результат 9 для аргументов (28, 3)
PassФункция возвращает верный результат 2 для аргументов (12, 5)
PassФункция возвращает верный результат 6 для аргументов (19, 3)
PassФункция возвращает верный результат 18 для аргументов (90, 5)
PassФункция возвращает верный результат 10 для аргументов (40, 4)
PassФункция возвращает верный результат 13 для аргументов (13, 1)
PassФункция возвращает верный результат 36 для аргументов (73, 2)
PassФункция возвращает верный результат 8 для аргументов (48, 6)
PassФункция возвращает верный результат 9 для аргументов (37, 4)
PassФункция возвращает верный результат 6 для аргументов (48, 8)
PassФункция возвращает верный результат 12 для аргументов (49, 4)
PassФункция возвращает верный результат 14 для аргументов (59, 4)
PassФункция возвращает верный результат 17 для аргументов (89, 5)
PassФункция возвращает верный результат 5 для аргументов (45, 9)
PassФункция возвращает верный результат 3 для аргументов (25, 7)
PassФункция возвращает верный результат 3 для аргументов (27, 8)
PassФункция возвращает верный результат 0 для аргументов (1, 3)
