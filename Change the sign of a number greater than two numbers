Write a function with the name changeMax, which takes two numbers a and b as arguments, changes the sign of the greater of the numbers to the opposite and returns this number.
If the numbers are equal, return the message "The numbers are equal."
Example:
function changeMax (9, 4) must return -9;
function changeMax (1, 6) must return -6;
function changeMax (0, 4) must return -4;
function changeMax (-1, -3) must return 1;
function changeMax (4, 4) must return "The numbers are equal."

Напишите функцию с именем changeMax, которая принимает два числа a и b в качестве аргументов, меняет знак большего из чисел на противоположный и возвращает это число. 
Если числа равны, вернуть сообщение "The numbers are equal".
Пример:
функция changeMax(9, 4) должна возвратить -9;
функция changeMax(1, 6) должна возвратить -6;
функция changeMax(0, 4) должна возвратить -4;
функция changeMax(-1, -3) должна возвратить 1;
функция changeMax(4, 4) должна возвратить "The numbers are equal".

CODE 1:
function changeMax(a, b){
if (a > b) return -a
else if (b > a) return -b
else return "The numbers are equal"
}

Все тесты пройдены успешно
PassСоздана функция changeMax
PassФункция для аргументов (2, -4) возвращает верный результат -2
PassФункция для аргументов (-4, 1) возвращает верный результат -1
PassФункция для аргументов (8, -2) возвращает верный результат -8
PassФункция для аргументов (5, 9) возвращает верный результат -9
PassФункция для аргументов (-2, 1) возвращает верный результат -1
PassФункция для аргументов (0, 1) возвращает верный результат -1
PassФункция для аргументов (-1, -4) возвращает верный результат 1
PassФункция для аргументов (3, -5) возвращает верный результат -3
PassФункция для аргументов (-3, 6) возвращает верный результат -6
PassФункция для аргументов (-9, 3) возвращает верный результат -3
PassФункция для аргументов (1, 7) возвращает верный результат -7
PassФункция для аргументов (2, -10) возвращает верный результат -2
PassФункция для аргументов (-5, -3) возвращает верный результат 3
PassФункция для аргументов (3, -4) возвращает верный результат -3
PassФункция для аргументов (-2, 8) возвращает верный результат -8
PassФункция для аргументов (4, -6) возвращает верный результат -4
PassФункция для аргументов (3, 7) возвращает верный результат -7
PassФункция для аргументов (-7, -1) возвращает верный результат 1
PassФункция для аргументов (0, -9) возвращает верный результат 0
PassФункция для аргументов (-1, -5) возвращает верный результат 1
PassФункция для аргументов (2, -6) возвращает верный результат -2
PassФункция для аргументов (-1, -10) возвращает верный результат 1
PassФункция для аргументов (-8, 6) возвращает верный результат -6
PassФункция для аргументов (4, 9) возвращает верный результат -9
PassФункция для аргументов (5, 3) возвращает верный результат -5
PassФункция для аргументов (8, -1) возвращает верный результат -8
PassФункция для аргументов (9, 0) возвращает верный результат -9
PassФункция для аргументов (-7, 8) возвращает верный результат -8
PassФункция для аргументов (-8, 0) возвращает верный результат 0
PassФункция для аргументов (1, -10) возвращает верный результат -1
PassФункция для аргументов (5, 5) возвращает верный результат The numbers are equal
