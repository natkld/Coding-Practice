Given a positive integer. Get an array of its digits written in reverse order (from last digit to first digit).
Write a function named arrayOfDigits, which takes the number n as an argument and returns an array of its digits in reverse order.
Split, reverse methods are not allowed. In the solution, use the do while loop.
Examples:

function arrayOfDigits (123456) must return [6, 5, 4, 3, 2, 1];
function arrayOfDigits (1000) must return [0, 0, 0, 1];
function arrayOfDigits (1) must return [1].

Дано целое положительное число. Получите массив его цифр, записанных в обратном порядке (от последней цифры к первой).
Напишите функцию с именем arrayOfDigits, которая принимает в качестве аргумента число n и возвращает массив его цифр в обратном порядке. 
Запрещено использование методов split, reverse. В решении используйте цикл do while.
Примеры:

функция arrayOfDigits(123456) должна возвратить [6, 5, 4, 3, 2, 1];
функция arrayOfDigits(1000) должна возвратить [0, 0, 0, 1];
функция arrayOfDigits(1) должна возвратить [1].

CODE 1:
function arrayOfDigits(n){
let arr = [];
let b;
let sum = 0;
do {
 b = n % 10;
  arr.push(b);
  n = (n - b) / 10;
} while(n >= 1)
return(arr);
}

Все тесты пройдены успешно
PassСоздана функция arrayOfDigits
PassИспользуется оператор do while
PassНе используются методы строк и массивов
PassФункция возвращает верный результат [9,9,9,3,6] для аргумента 63999
PassФункция возвращает верный результат [9,9,8,1,8] для аргумента 81899
PassФункция возвращает верный результат [7,5,6,8,3] для аргумента 38657
PassФункция возвращает верный результат [9,6,1,6,3] для аргумента 36169
PassФункция возвращает верный результат [6,6,7,8,2] для аргумента 28766
PassФункция возвращает верный результат [1,4,4,9,3] для аргумента 39441
PassФункция возвращает верный результат [7,2,9,8,7] для аргумента 78927
PassФункция возвращает верный результат [3,4,6,2,9] для аргумента 92643
PassФункция возвращает верный результат [1,1,0,4] для аргумента 4011
PassФункция возвращает верный результат [6,6,8,0,5] для аргумента 50866
PassФункция возвращает верный результат [0,1,8,7,8] для аргумента 87810
PassФункция возвращает верный результат [8,0,1,8,7] для аргумента 78108
PassФункция возвращает верный результат [3,9,2,5,6] для аргумента 65293
PassФункция возвращает верный результат [0,8,4,6,9] для аргумента 96480
PassФункция возвращает верный результат [3,2,3,8,8] для аргумента 88323
PassФункция возвращает верный результат [8,0,3,7] для аргумента 7308
PassФункция возвращает верный результат [0,7,8,4,4] для аргумента 44870
PassФункция возвращает верный результат [3,8,6,8,4] для аргумента 48683
PassФункция возвращает верный результат [2,1,3,7,7] для аргумента 77312
PassФункция возвращает верный результат [1,0,7,1,4] для аргумента 41701
