Find the least of the two given numbers.
Write the min function, which takes two numbers a, b as arguments and returns the smallest number.
Example:
the min (13, 45) function must return 13;
the min (12, 12) function must return 12;
the min (21, 12) function must return 12.

Найти меньшее из двух данных чисел.
Напишите функцию min, которая принимает два числа a, b в качестве аргументов и возвращает наименьшее число.
Пример:
функция min(13, 45) должна возвратить 13;
функция min(12, 12) должна возвратить 12;
функция min(21, 12) должна возвратить 12.

CODE 1:
function min(a, b){
return Math.min(a, b)
}

Все тесты пройдены успешно
PassСоздана функция min
PassФункция возвращает для аргументов (-22, 5) верный результат -22
PassФункция возвращает для аргументов (2, -7) верный результат -7
PassФункция возвращает для аргументов (-3, 22) верный результат -3
PassФункция возвращает для аргументов (2, -15) верный результат -15
PassФункция возвращает для аргументов (18, 18) верный результат 18
PassФункция возвращает для аргументов (2, -21) верный результат -21
PassФункция возвращает для аргументов (-20, -20) верный результат -20
PassФункция возвращает для аргументов (5, -8) верный результат -8
PassФункция возвращает для аргументов (11, -6) верный результат -6
PassФункция возвращает для аргументов (-25, -4) верный результат -25
PassФункция возвращает для аргументов (-24, 8) верный результат -24
PassФункция возвращает для аргументов (21, 12) верный результат 12
PassФункция возвращает для аргументов (11, -25) верный результат -25
PassФункция возвращает для аргументов (22, -22) верный результат -22
PassФункция возвращает для аргументов (13, 9) верный результат 9
PassФункция возвращает для аргументов (-3, 1) верный результат -3
PassФункция возвращает для аргументов (-16, 22) верный результат -16
PassФункция возвращает для аргументов (-8, 6) верный результат -8
PassФункция возвращает для аргументов (-21, 13) верный результат -21
PassФункция возвращает для аргументов (-23, -20) верный результат -23
PassФункция возвращает для аргументов (-16, -17) верный результат -17
PassФункция возвращает для аргументов (19, 20) верный результат 19
PassФункция возвращает для аргументов (-7, 20) верный результат -7
PassФункция возвращает для аргументов (-5, 5) верный результат -5
PassФункция возвращает для аргументов (-18, 6) верный результат -18
PassФункция возвращает для аргументов (-6, 16) верный результат -6
PassФункция возвращает для аргументов (-4, 20) верный результат -4
PassФункция возвращает для аргументов (-23, -9) верный результат -23
PassФункция возвращает для аргументов (15, 3) верный результат 3
PassФункция возвращает для аргументов (12, 3) верный результат 3
PassФункция возвращает для аргументов (-22, 8) верный результат -22
PassФункция возвращает для аргументов (-19, 7) верный результат -19
PassФункция возвращает для аргументов (18, -8) верный результат -8
PassФункция возвращает для аргументов (2, 6) верный результат 2
PassФункция возвращает для аргументов (-13, -20) верный результат -20
PassФункция возвращает для аргументов (-3, 24) верный результат -3
PassФункция возвращает для аргументов (5, -19) верный результат -19
PassФункция возвращает для аргументов (-25, 20) верный результат -25
PassФункция возвращает для аргументов (0, -5) верный результат -5
PassФункция возвращает для аргументов (1, -9) верный результат -9
PassФункция возвращает для аргументов (12, 24) верный результат 12
PassФункция возвращает для аргументов (13, 2) верный результат 2
PassФункция возвращает для аргументов (6, -14) верный результат -14
PassФункция возвращает для аргументов (-3, -19) верный результат -19
PassФункция возвращает для аргументов (10, -23) верный результат -23
PassФункция возвращает для аргументов (2, 0) верный результат 0
PassФункция возвращает для аргументов (12, -10) верный результат -10
PassФункция возвращает для аргументов (-25, -16) верный результат -25
PassФункция возвращает для аргументов (-1, 22) верный результат -1
PassФункция возвращает для аргументов (4, -19) верный результат -19
