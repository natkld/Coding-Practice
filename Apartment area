There is an apartment plan consisting of 4 rooms. The spaces have a square shape, and respectively dimensions: a, b, c, d.
plan
Write a function called square, which takes four numbers a, b, c, d as arguments and returns the total area of ​ ​ the apartment.
Examples:
square (1, 2, 4, 8) must return 85;
square (1, 3, 8, 10) must return 174.

Имеется план квартиры, состоящей из 4-х помещений. Помещения имеют квадратную форму, и соответственно размеры: a, b, c, d.
plan
Напишите функцию с именем square, которая принимает четыре числа a, b, c, d в качестве аргументов и возвращает общую площадь квартиры.
Примеры:
функция square(1, 2, 4, 8) должна возвратить 85;
функция square(1, 3, 8, 10) должна возвратить 174.

CODE 1:
function square(a, b, c, d){
 return (Math.pow(a, 2) + Math.pow(b, 2) + Math.pow(c, 2) + Math.pow(d, 2))
}

Все тесты пройдены успешно
PassСоздана функция square
PassФункция возвращает верный результат 245 для аргументов (3, 10, 6, 10)
PassФункция возвращает верный результат 170 для аргументов (10, 6, 5, 3)
PassФункция возвращает верный результат 63 для аргументов (7, 1, 3, 2)
PassФункция возвращает верный результат 189 для аргументов (6, 5, 8, 8)
PassФункция возвращает верный результат 233 для аргументов (10, 6, 4, 9)
PassФункция возвращает верный результат 34 для аргументов (1, 2, 2, 5)
PassФункция возвращает верный результат 242 для аргументов (10, 6, 9, 5)
PassФункция возвращает верный результат 12 для аргументов (1, 1, 3, 1)
PassФункция возвращает верный результат 188 для аргументов (1, 5, 9, 9)
PassФункция возвращает верный результат 185 для аргументов (6, 8, 7, 6)
PassФункция возвращает верный результат 118 для аргументов (2, 8, 1, 7)
PassФункция возвращает верный результат 55 для аргументов (2, 1, 7, 1)
PassФункция возвращает верный результат 271 для аргументов (3, 9, 9, 10)
PassФункция возвращает верный результат 298 для аргументов (9, 6, 10, 9)
PassФункция возвращает верный результат 172 для аргументов (1, 9, 3, 9)
PassФункция возвращает верный результат 117 для аргументов (7, 4, 4, 6)
PassФункция возвращает верный результат 155 для аргументов (1, 9, 8, 3)
PassФункция возвращает верный результат 225 для аргументов (10, 6, 8, 5)
PassФункция возвращает верный результат 122 для аргументов (6, 5, 5, 6)
PassФункция возвращает верный результат 156 для аргументов (4, 2, 10, 6)
PassФункция возвращает верный результат 171 для аргументов (4, 9, 5, 7)
PassФункция возвращает верный результат 209 для аргументов (3, 6, 10, 8)
PassФункция возвращает верный результат 165 для аргументов (8, 4, 7, 6)
PassФункция возвращает верный результат 16 для аргументов (2, 2, 2, 2)
PassФункция возвращает верный результат 51 для аргументов (5, 4, 3, 1)
PassФункция возвращает верный результат 162 для аргументов (3, 2, 10, 7)
PassФункция возвращает верный результат 107 для аргументов (9, 3, 4, 1)
PassФункция возвращает верный результат 90 для аргументов (8, 4, 3, 1)
PassФункция возвращает верный результат 200 для аргументов (6, 6, 8, 8)
PassФункция возвращает верный результат 138 для аргументов (5, 2, 10, 3)
PassФункция возвращает верный результат 78 для аргументов (7, 2, 3, 4)
PassФункция возвращает верный результат 42 для аргументов (2, 3, 5, 2)
PassФункция возвращает верный результат 279 для аргументов (7, 10, 7, 9)
PassФункция возвращает верный результат 161 для аргументов (6, 3, 4, 10)
PassФункция возвращает верный результат 138 для аргументов (9, 4, 4, 5)
PassФункция возвращает верный результат 289 для аргументов (8, 10, 5, 10)
PassФункция возвращает верный результат 27 для аргументов (1, 1, 3, 4)
PassФункция возвращает верный результат 123 для аргументов (5, 8, 3, 5)
PassФункция возвращает верный результат 174 для аргументов (2, 9, 8, 5)
PassФункция возвращает верный результат 304 для аргументов (10, 2, 10, 10)
PassФункция возвращает верный результат 91 для аргументов (7, 5, 4, 1)
PassФункция возвращает верный результат 78 для аргументов (4, 1, 6, 5)
PassФункция возвращает верный результат 46 для аргументов (5, 2, 1, 4)
PassФункция возвращает верный результат 58 для аргументов (2, 2, 1, 7)
PassФункция возвращает верный результат 90 для аргументов (1, 4, 3, 8)
PassФункция возвращает верный результат 123 для аргументов (3, 8, 5, 5)
PassФункция возвращает верный результат 232 для аргументов (8, 2, 8, 10)
PassФункция возвращает верный результат 147 для аргументов (9, 7, 1, 4)
PassФункция возвращает верный результат 152 для аргументов (6, 4, 6, 8)
PassФункция возвращает верный результат 99 для аргументов (3, 5, 7, 4)
