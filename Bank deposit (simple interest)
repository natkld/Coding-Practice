The depositor put $1,000 in the bank. This amount is increased annually by p (p > 0) percent of the initial deposit amount. How many years will the account have $1,500?
Write a function named bankPercent that accepts the number p (bank interest) and calculates how many years the account will be $1,500.
In the solution, use the do while loop.
Examples:

function bankPercent (10) must return 5;
function bankPercent (5) must return 10;
function bankPercent (13) must return 4.

Вкладчик положил 1000 долларов в банк. Ежегодно эта сумма увеличивается на p (p > 0) процентов от начальной суммы вклада. Через сколько лет на счету будет 1500 долларов?
Напишите функцию с именем bankPercent, которая принимает число p (банковский процент) и вычисляет, через сколько лет на счету будет 1500 долларов.
В решении используйте цикл do while.
Примеры:

функция bankPercent(10) должна возвратить 5;
функция bankPercent(5) должна возвратить 10;
функция bankPercent(13) должна возвратить 4.

CODE 1:
function bankPercent(p){
  let sum = 1000;
  let year = 0;
  let interest = sum * p / 100;
  do {
    sum = sum + interest;
    year++;
  } while (sum < 1500)
  return year;
}

Создана функция bankPercent
PassИспользуется оператор do while
PassФункция возвращает верный результат 4 для аргумента 14
PassФункция возвращает верный результат 2 для аргумента 25
PassФункция возвращает верный результат 10 для аргумента 5
PassФункция возвращает верный результат 2 для аргумента 26
PassФункция возвращает верный результат 3 для аргумента 22
PassФункция возвращает верный результат 50 для аргумента 1
PassФункция возвращает верный результат 2 для аргумента 29
PassФункция возвращает верный результат 3 для аргумента 23
PassФункция возвращает верный результат 10 для аргумента 5
PassФункция возвращает верный результат 50 для аргумента 1
PassФункция возвращает верный результат 2 для аргумента 27
PassФункция возвращает верный результат 5 для аргумента 10
PassФункция возвращает верный результат 4 для аргумента 14
PassФункция возвращает верный результат 7 для аргумента 8
PassФункция возвращает верный результат 2 для аргумента 25
PassФункция возвращает верный результат 4 для аргумента 15
PassФункция возвращает верный результат 3 для аргумента 17
PassФункция возвращает верный результат 17 для аргумента 3
PassФункция возвращает верный результат 5 для аргумента 12
PassФункция возвращает верный результат 13 для аргумента 4
