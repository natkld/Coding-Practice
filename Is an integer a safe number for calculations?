Write the function safeNumber, which takes the integer x, and return true if the variable x contains a safe integer value, and false otherwise.
Examples:
The function safeNumber (20,000) must return true;
function safeNumber (9007199254740991) must return true;
function safeNumber (9007199254740992) must return false.

Напишите функцию safeNumber, которая принимает целое число x, и возвращает true, если переменная x содержит безопасное целочисленное значение, и false - в противном случае.
Примеры:
функция safeNumber(20000) должна возвратить true;
функция safeNumber(9007199254740991) должна возвратить true;
функция safeNumber(9007199254740992) должна возвратить false.

CODE 1:
function safeNumber(x){
return (Number.isSafeInteger(x))? true : false
}

Все тесты пройдены успешно
PassСоздана функция safeNumber
PassФункция возвращает верный результат "true" для аргумента 9007199254740991
PassФункция возвращает верный результат "true" для аргумента (2**53 - 2)
PassФункция возвращает верный результат "true" для аргумента (2**53 - 1)
PassФункция возвращает верный результат "false" для аргумента (2**53)
PassФункция возвращает верный результат "false" для аргумента (2**53 +1)
PassФункция возвращает верный результат "true" для аргумента -9007199254740991
PassФункция возвращает верный результат "true" для аргумента -(2**53 - 1)
PassФункция возвращает верный результат "true" для аргумента -(2**53)
PassФункция возвращает верный результат "false" для аргумента -(2**53 + 1)
PassФункция возвращает верный результат "false" для аргумента -(2**53 + 2)
PassФункция возвращает верный результат "false" для аргумента -(2**55)
PassФункция возвращает верный результат "true" для аргумента -(2**52)
PassФункция возвращает верный результат "false" для аргумента (2**55)
PassФункция возвращает верный результат "true" для аргумента (2**52)
PassФункция возвращает верный результат "false" для аргумента (Infinity)
PassФункция возвращает верный результат "false" для аргумента (-Infinity)
