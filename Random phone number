При тестировании страницы регистрации пользователя вам надо вам надо протестировать поле "Phone number". 
Вам надо сделать множество тестов, и вы решили создавать случайные телефонные номера одинадцатизначные числа.
Номер телефона должен начинаться с символа "+", первая цифра номера должна быть 1.
Напишите функцию с именем createPhoneNumber, которая возвращает случайный номер телефона в виде строки длиной 12,
начинающейся с "+1" и случайных цифр.
Указание:
- Создайте строку, содержащую первые два символа: "+1";
- Создайте цикл, который выполняется 10 раз;
- В теле цикла получите случайное число от 0 до 9 и прибавьте это число к строке.
Пример:
функция createPhoneNumber возвращает номер телефона в виде "+12345678910".

My code is:
function createPhoneNumber(n){
str = '+1';
  return str + Math.floor(Math.random () * 8999999999 + Math.pow(10, 9));
}

or

function createPhoneNumber(n){
str = '+1';
  return str + Math.floor(Math.random () * 8999999999 + 1000000000));
}
