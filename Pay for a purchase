Claudia buys sweets in the store, her purchase is $ XX.XX. She's gonna pay cash, she's got a bunch of dollar notes,
and there are no coins. What is the minimum amount of dollars Claudia must give to the cashier to pay for the purchase?
Write a function with the name numberOfDollars, which takes the number cost (purchase amount) and as an argument and returns the minimum integer number of dollars,
which must be handed over to the cashier for the purchase.
Examples:
function numberOfDollars (12.75) must return 13;
function numberOfDollars (15.1) must return 16.

Клавдия покупает сладости в магазине, ее покупка составляет $XX.XX. Она собирается расплатиться наличными, у нее есть пачка долларовых банкнот, 
и нет никаких монет. Какое минимальное количество долларов должна Клавдия передать кассиру, чтобы расплатиться за покупку?
Напишите функцию с именем numberOfDollars, которая принимает число cost (сумма покупки) и в качестве аргумента и возвращает минимальное целое количество долларов,
которое надо передать кассиру за покупку.
Примеры:
функция numberOfDollars(12.75) должна возвратить 13;
функция numberOfDollars(15.1) должна возвратить 16.

CODE 1:
function numberOfDollars(cost){
  return Math.ceil(cost)
}

Все тесты пройдены успешно
PassСоздана функция numberOfDollars
PassФункция возвращает верный результат 11 для аргумента 10.46
PassФункция возвращает верный результат 16 для аргумента 15.38
PassФункция возвращает верный результат 14 для аргумента 13.12
PassФункция возвращает верный результат 15 для аргумента 14.35
PassФункция возвращает верный результат 20 для аргумента 19.2
PassФункция возвращает верный результат 17 для аргумента 16.59
PassФункция возвращает верный результат 6 для аргумента 5.06
PassФункция возвращает верный результат 15 для аргумента 14.86
PassФункция возвращает верный результат 16 для аргумента 15.66
PassФункция возвращает верный результат 19 для аргумента 18.09
PassФункция возвращает верный результат 25 для аргумента 24.56
PassФункция возвращает верный результат 35 для аргумента 34.57
PassФункция возвращает верный результат 32 для аргумента 31.71
PassФункция возвращает верный результат 25 для аргумента 24.88
PassФункция возвращает верный результат 3 для аргумента 2.4
PassФункция возвращает верный результат 22 для аргумента 21.64
PassФункция возвращает верный результат 40 для аргумента 39.98
PassФункция возвращает верный результат 12 для аргумента 11.36
PassФункция возвращает верный результат 9 для аргумента 8.02
PassФункция возвращает верный результат 19 для аргумента 18.59
