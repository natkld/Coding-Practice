Given the number n > 0. Find all divisors of this number. A divisor is a number by which a given number is divided without a remainder.
Write a function named dividers that takes the number n and returns the divisors of that number as an array.

Дано число n > 0. Найдите все делители этого числа. Делитель - это число, на которое данное число делится без остатка.
Напишите функцию с именем dividers, которая принимает число n и возвращает делители этого числа в виде массива.

CODE:
function dividers(n){
let arr = [];
for (let i = 1; i <= n; i++){
 if (n % i === 0)
 arr.push(i)
}
return arr
}

console.log(dividers(1))  // [1];
console.log(dividers(2))  // [1, 2];
console.log(dividers(3))  // [1, 3];
console.log(dividers(4))  // [1, 2, 4];
console.log(dividers(12)) // [1, 2, 3, 4, 6, 12]
